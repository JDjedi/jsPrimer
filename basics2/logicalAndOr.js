let temp = 55


// and - checks statement for both to be true
if (temp >= 60 && temp <= 90) {
    console.log('It is pretty outside...')
}

// or - as long as at least one side is true the statment resolves to true
if (temp <= 31 || temp >= 120) {
    console.log('It is pretty outside')
}